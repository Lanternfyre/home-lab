---
- name: restart systemd-resolved
  ansible.builtin.systemd:
    name: systemd-resolved
    state: restarted

# NOTE: there is deliberately no `restart multipathd` handler. That restart is
# gated inline in tasks/main.yml on there being no live iSCSI sessions, and a
# handler cannot express that per-host condition safely.
