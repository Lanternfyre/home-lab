# Own identity, no Role, token not mounted. cloudflared talks to Cloudflare and
# to Envoy; it has no business with the Kubernetes API.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: cloudflared
  namespace: cloudflare-tunnel
  labels:
    app.kubernetes.io/name: cloudflared
automountServiceAccountToken: false
